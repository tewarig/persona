# Saved voices are personal — keep the directory and its README, not the specs.
# Remove these two lines if you ever want to ship a voice with the skill.
personas/*.md
!personas/README.md

# macOS
.DS_Store
